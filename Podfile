# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VIP Base' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VIP Base

	pod 'Alamofire'
  pod 'SDWebImage'
  pod 'IQKeyboardManagerSwift'
  
  target 'VIP BaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VIP BaseUITests' do
    # Pods for testing
  end

end
